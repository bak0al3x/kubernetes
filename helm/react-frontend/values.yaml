# Default values for react-frontend.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

name: "hello-react"

image:
  name: "example-react-app"
  tag: "latest"

ingress:
  host: "hello-react.kube"